"use client";

// import dayjs from "dayjs";
// import { useState } from "react";
// import { Workout } from "@/components/Workout";
// import DatePicker, { OnChange } from "@/components/DatePicker";

export default function Planner() {
  // const [selectedDate, setSelectedDate] = useState(dayjs());

  // const handleChange: OnChange = (selectedDate) => {
  //   selectedDate && setSelectedDate(selectedDate);
  // };

  return (
    <div>
      {/* <DatePicker date={selectedDate} onChange={handleChange} /> */}
      {/* <Workout date={selectedDate} /> */}
    </div>
  );
}
0;
